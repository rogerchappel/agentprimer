{
  "name": "agentprimer",
  "version": "0.1.0",
  "description": "Local-first repo onboarding packet CLI for coding agents.",
  "type": "module",
  "bin": {
    "agentprimer": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "smoke": "npm run build && node dist/index.js scan fixtures/node-cli --out /tmp/agentprimer-smoke.md && node dist/index.js suggest-task fixtures/sparse-repo --max-risk low",
    "package:smoke": "npm pack --dry-run",
    "release:check": "npm test && npm run check && npm run build && npm run smoke && npm run package:smoke"
  },
  "keywords": [
    "agent",
    "cli",
    "onboarding",
    "repository",
    "handoff"
  ],
  "author": "Roger Chappel",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
